fun main() {
    val firstLine = readln()
    val inputs = firstLine.split(" ").map { it.toInt() }
    val n = inputs[0]
    val m = inputs[1]
    while(true) {
        val line = readlnOrNull() ?: break
        val line.split(" ")
    }
}